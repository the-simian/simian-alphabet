const unit = 15;

const letterWidth = unit * 5;
const letterHeight = unit * 10;
const halfWayWidth = letterWidth / 2;

const kerning = letterWidth * 1.1;

export {
  letterWidth,
  letterHeight,
  halfWayWidth,
  unit,
  kerning
}
